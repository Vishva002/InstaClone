# InstaClone
From my React.js Learning to create the insta clone for frontend only
json server install command: npm install json-server
To run the server: npx json-server --watch db/db.json --port 3001
